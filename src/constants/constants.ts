export const PAGE_SIZE = 8;

export const LIGHT_MODE = "light",
	DARK_MODE = "dark",
	AUTO_MODE = "auto";
export const DEFAULT_THEME = AUTO_MODE;

// Altura do banner unidade: vh
export const BANNER_HEIGHT = 17;
export const BANNER_HEIGHT_EXTEND = 10;
export const BANNER_HEIGHT_HOME = BANNER_HEIGHT + BANNER_HEIGHT_EXTEND;

// A altura que o painel principal sobrepõe o banner, unidade: rem (maior = conteúdo mais perto do head/navbar)
export const MAIN_PANEL_OVERLAPS_BANNER_HEIGHT = 7.5;

// Largura da página: rem
export const PAGE_WIDTH = 75;

// Padding vertical do container do body (rem) — reduzido para menos espaço em cima/embaixo
export const BODY_PADDING_Y = 3;
export const BODY_PADDING_TOP = 0;
// Margin-top do body (rem)
export const BODY_MARGIN_TOP = 0.5;
